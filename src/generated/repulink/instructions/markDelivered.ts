/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { REPULINK_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const MARK_DELIVERED_DISCRIMINATOR = new Uint8Array([
  240, 118, 188, 142, 64, 85, 107, 18,
]);

export function getMarkDeliveredDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MARK_DELIVERED_DISCRIMINATOR,
  );
}

export type MarkDeliveredInstruction<
  TProgram extends string = typeof REPULINK_PROGRAM_ADDRESS,
  TAccountFreelancer extends string | AccountMeta<string> = string,
  TAccountJob extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFreelancer extends string
        ? ReadonlySignerAccount<TAccountFreelancer> &
            AccountSignerMeta<TAccountFreelancer>
        : TAccountFreelancer,
      TAccountJob extends string ? WritableAccount<TAccountJob> : TAccountJob,
      ...TRemainingAccounts,
    ]
  >;

export type MarkDeliveredInstructionData = {
  discriminator: ReadonlyUint8Array;
  deliveryHash: ReadonlyUint8Array;
};

export type MarkDeliveredInstructionDataArgs = {
  deliveryHash: ReadonlyUint8Array;
};

export function getMarkDeliveredInstructionDataEncoder(): FixedSizeEncoder<MarkDeliveredInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["deliveryHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: MARK_DELIVERED_DISCRIMINATOR }),
  );
}

export function getMarkDeliveredInstructionDataDecoder(): FixedSizeDecoder<MarkDeliveredInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["deliveryHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getMarkDeliveredInstructionDataCodec(): FixedSizeCodec<
  MarkDeliveredInstructionDataArgs,
  MarkDeliveredInstructionData
> {
  return combineCodec(
    getMarkDeliveredInstructionDataEncoder(),
    getMarkDeliveredInstructionDataDecoder(),
  );
}

export type MarkDeliveredInput<
  TAccountFreelancer extends string = string,
  TAccountJob extends string = string,
> = {
  freelancer: TransactionSigner<TAccountFreelancer>;
  job: Address<TAccountJob>;
  deliveryHash: MarkDeliveredInstructionDataArgs["deliveryHash"];
};

export function getMarkDeliveredInstruction<
  TAccountFreelancer extends string,
  TAccountJob extends string,
  TProgramAddress extends Address = typeof REPULINK_PROGRAM_ADDRESS,
>(
  input: MarkDeliveredInput<TAccountFreelancer, TAccountJob>,
  config?: { programAddress?: TProgramAddress },
): MarkDeliveredInstruction<TProgramAddress, TAccountFreelancer, TAccountJob> {
  // Program address.
  const programAddress = config?.programAddress ?? REPULINK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    freelancer: { value: input.freelancer ?? null, isWritable: false },
    job: { value: input.job ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.freelancer),
      getAccountMeta(accounts.job),
    ],
    data: getMarkDeliveredInstructionDataEncoder().encode(
      args as MarkDeliveredInstructionDataArgs,
    ),
    programAddress,
  } as MarkDeliveredInstruction<
    TProgramAddress,
    TAccountFreelancer,
    TAccountJob
  >);
}

export type ParsedMarkDeliveredInstruction<
  TProgram extends string = typeof REPULINK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    freelancer: TAccountMetas[0];
    job: TAccountMetas[1];
  };
  data: MarkDeliveredInstructionData;
};

export function parseMarkDeliveredInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedMarkDeliveredInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { freelancer: getNextAccount(), job: getNextAccount() },
    data: getMarkDeliveredInstructionDataDecoder().decode(instruction.data),
  };
}
